//import { 
    //Account, 
    //Clarinet,
    //Chain, 
    //types,
  //} from '../utils/helpers';
//import { ExecutorDao } from '../models/executor-dao-model';

//Clarinet.test({
    //name: '😢 SDE0060Governance',
    //async fn(chain: Chain, accounts: Map<string, Account>) {
      //let deployer = accounts.get('deployer')!;
      //let Dao = new ExecutorDao(chain);
      //let Governance = new SDE000Governance(chain);
      //let data: any = null;
      //let invalidStartHeight: number = 50; 
    //},


//});

//Clarinet.test({
    //name: '😃 SDE0060Governance',
    //async fn(chain: Chain, accounts: Map<string, Account>) {
      //let deployer = accounts.get('deployer')!;
      //let Dao = new ExecutorDao(chain);
      //let Governance = new SDE000Governance(chain);
      //let data: any = null;
      //let validStartHeight: number = 150;
    //},

//});
